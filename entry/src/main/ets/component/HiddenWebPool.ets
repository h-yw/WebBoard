/**
 * HiddenWebPool was an experimental component for offscreen Web preloading.
 *
 * In ArkUI API 12+, Web components must be part of the visible component tree
 * to render content. Offscreen positioning does not reliably trigger Web
 * engine rendering. Therefore, this component is currently not in use.
 *
 * The WebPreloader singleton handles access tracking (recently used apps)
 * which is the foundation for future preloading when platform support matures.
 */

export {}
