# 优化迭代

* WebBoard 是一个 HarmonyOS 应用，用于管理和渲染 HTML 内容。
* 所有优化迭代内容，请搜索知识库和HarmonyOS的能力，寻找最佳实践。
* 如果没有合适的解决方案，请标记后跳过，并说明原因。

## 中文应用名

结合应用功能，尝试为应用起一个中文名字称，目前英文名字是“WebBoard”。

## 文件导入重构

1. 当前是通过将html复制到应用沙箱中，后续从沙箱读取html并加载。
2. 请搜索知识库和HarmonyOS的能力，是否可以只需导入时记录文件位置，后续只需通过记录位置路径加载html。


## 网络导入优化

1. 当前网络导入是下载的本地；
2. 请搜索知识库和HarmonyOS的能力，是否可以通过网络直接加载，不必下载到本地；

## 数据存储

1. 基于上述优化，导入数据以sqlite的方式存储本地；
2. 存储数据时，请搜索知识库和HarmonyOS的能力，是否可以基于sqlite存储数据。

## 通用能力封装

1. 请搜索知识库和HarmonyOS的能力，封装通用Toast
2. 请搜索知识库和HarmonyOS的能力，封装通用Loading
3. 请搜索知识库和HarmonyOS的能力，封装通用Dialog

## UI/UX

* 基于上述优化，优化UI/UX。